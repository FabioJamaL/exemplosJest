import {main} from "./bee1013"
import {soma} from "/c/Users/fabioDev/Documents/Fabio/EXERCICIOS/tests/soma"

import {main} from "./bee1013"

describe('cenarios de teste', () => {
    const cenarios = [...]

    cenarios.forEach((cenario, 1) => {
        test('Caso #') + i, () => {
            let {lines, result} = cenario;

            let output = main(lines);

            expect(output).toBe(result);

        });

    })   

    

